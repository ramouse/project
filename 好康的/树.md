# 链表

链表是一种用于存储数据的数据结构，通过如链条一般的指针来连接元素。它的特点是插入与删除数据十分方便，但寻找与读取数据的表现欠佳。





## 与数组的区别

链表和数组都可用于存储数据。与链表不同，数组将所有元素按次序依次存储。不同的存储结构令它们有了不同的优势：

- 链表因其链状的结构，能方便地删除、插入数据，操作次数是 $O(1)$。但也因为这样，寻找、读取数据的效率不如数组高，在随机访问数据中的操作次数是 $O(n)$。
- 数组可以方便地寻找并读取数据，在随机访问中操作次数是 $O(1)$。但删除、插入的操作次数是 $O(n)$ 次。





# 树

## 定义

一个没有固定根结点的树称为**无根树**（unrooted tree）。无根树有几种等价的形式化定义：

- 有 $n$ 个结点，$n-1$ 条边的连通无向图
- 无向无环的连通图
- 任意两个结点之间有且仅有一条简单路径的无向图
- 任何边均为桥的连通图
- 没有圈，且在任意不同两点间添加一条边之后所得含唯一的一个圈的圈

在无根树的基础上，指定一个结点称为**根**，则形成一棵**有根树**（rooted tree）。有根树在很多时候仍以无向图表示，只是规定了结点之间的上下级关系。





## 有关树的定义

### 适用于无根树和有根树

- **森林**（forest）：每个连通分量（连通块）都是树的图。按照定义，一棵树也是森林。
- **生成树**（spanning tree）：一个连通无向图的生成子图，同时要求是树。也即在图的边集中选择 $n-1$ 条，将所有顶点连通。
- **无根树的叶结点**（leaf node）：度数不超过 1 的结点。
- **有根树的叶结点**（leaf node）：没有子结点的结点。





### 只适用于有根树

- **父亲**（parent node）：对于除根以外的每个结点，定义为从该结点到根路径上的第二个结点。根结点没有父结点。
- **祖先**（ancestor）：一个结点到根结点的路径上，除了它本身外的结点。根结点的祖先集合为空。
- **子结点**（child node）：如果 $u$ 是 $v$ 的父亲，那么 $v$ 是 $u$ 的子结点。子结点的顺序一般不加以区分，二叉树是一个例外。
- **结点的深度**（depth）：到根结点的路径上的边数。
- **树的高度**（height）：所有结点的深度的最大值。
- **兄弟**（sibling）：同一个父亲的多个子结点互为兄弟。
- **后代**（descendant）：子结点和子结点的后代。或者理解成：如果 $u$ 是 $v$ 的祖先，那么 $v$ 是 $u$ 的后代。
- **子树**（subtree）：删掉与父亲相连的边后，该结点所在的子图。





## 存储

### 邻接表

```cpp
vector<int> adj[N];

void build() {
    int n;
    cin >> n;
    for (int i = 0; i < n - 1; i++) {
        int u, v;
        cin >> u >> v;
        adj[u].push_back(v);
        adj[v].push_back(u);
    }
}
```



## 树的遍历

### 树上 DFS

在树上 DFS 是这样的一个过程：先访问根节点，然后分别访问根节点每个儿子的子树。可以用来求出每个节点的深度、父亲等信息。

cpp

```c++
void dfs(int u, int fa) {
    for (int v : adj[u]) {
        if (v == fa)
            continue;
        dfs(v, u);
    }
}

//二者等价

void dfs(int u,int fa){
    for(int v = 0;v<adj[u].size();u++){
        if(adj[u][v] == fa)
            continue;
        dfs(adj[u][v],u);
    }
}
```



### 树上 BFS

从树根开始，严格按照层次来访问节点。BFS 过程中也可以顺便求出各个节点的深度和父亲节点。

cpp

```c++
void bfs(int st) {
    vector<int> vis(N, 0);
    queue<int> q;
    q.push(st);
    vis[st] = 1; //标记已经访问过的
    while (!q.empty()) {
        int u = q.front();
        q.pop(); //访问后移除
        for (int v : adj[u]) {
            if (vis[v])
                continue;
            q.push(v); //将以u为节点的子节点存入队列，继续遍历
            vis[v] = 1; //标记
        }
    }
}
```



### 树的层序遍历

树层序遍历是指按照从根节点到叶子节点的层次关系，一层一层的横向遍历各个节点。根据 BFS 的定义可以知道，BFS 所得到的遍历顺序就是一种层序遍历。但层序遍历要求将不同的层次区分开来，所以其结果通常以二维数组的形式表示。

例如，下图的树的层序遍历的结果是 `[[1], [2, 3, 4], [5, 6]]`（每一层从左向右）。

## 树上 DFN

DFN序是通过深度优先搜索（DFS）对树进行遍历时生成的节点访问顺序。它的特点是祖先节点总是出现在子孙节点之前，因此在树的结构分析中具有重要作用。DFN序常用于解决树上的问题，例如最近公共祖先（LCA）的查询。

cpp

```c++
vector<int> in(N), out(N);
int tim = 0; // 时间戳

void dfn(int u, int fa) {
    in[u] = ++tim; //记录进入该节点的时间
    for (int v : adj[u]) {
        if (v == fa)
            continue;
        dfn(v, u);
    }
    out[u] = ++tim; //记录出该节点的时间
}

bool isAncestor(int u, int v) {
    return in[u] <= in[v] && out[v] <= out[u];
}//如果进的早且出的晚，可严格证明u是v的祖先
```



# 括号序列

定义一个**合法括号序列**（balanced bracket sequence）为仅由 `(` 和 `)` 构成的字符串：

- 空串 ε 是一个合法括号序列
- 如果 s 是合法括号序列，那么 (s) 也是合法括号序列
- 如果 s, t 都是合法括号序列，那么 st 也是合法括号序列

例如 `(())()` 是合法括号序列，而 `())` 不是。

有时候会有多种不同的括号，如 `[()]()`。这样的变种括号序列与朴素括号序列有相似的定义。

## P1241 括号序列 - 洛谷

### 解法一

cpp

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    vector<int> a(105, 0);
    cin >> s;
    
    for (int i = 0; i < s.size(); i++) {
        if (s[i] == ')') {
            for (int j = i - 1; j >= 0; j--) {
                if (s[j] == '(' && a[j] == 0) {
                    a[i] = 1;
                    a[j] = 1;
                    break;
                }
                if (s[j] == '[' && a[j] == 0) {
                    break;
                }
            }
        }
        
        if (s[i] == ']') {
            for (int j = i - 1; j >= 0; j--) {
                if (s[j] == '[' && a[j] == 0) {
                    a[i] = 1;
                    a[j] = 1;
                    break;
                }
                if (s[j] == '(' && a[j] == 0) {
                    break;
                }
            }
        }
    }
    
    for (int i = 0; i < s.size(); i++) {
        if (a[i] == 0) {
            if (s[i] == '(' || s[i] == ')')
                cout << "()";
            if (s[i] == '[' || s[i] == ']')
                cout << "[]";
        } else {
            cout << s[i];
        }
    }
    return 0;
}
```



### 解法二

cpp

```c++
#include <bits/stdc++.h>
using namespace std;

stack<int> st;

int main() {
    string s;
    cin >> s;
    vector<int> ok(105, 0);
    
    for (int i = 0, k; i < s.size(); i++) {
        if (s[i] == ']') {
            if (st.empty())
                continue;
            k = st.top();
            if (s[k] == '[') {
                ok[k] = ok[i] = 1;
                st.pop();
            }
        } else if (s[i] == ')') {
            if (st.empty())
                continue;
            k = st.top();
            if (s[k] == '(') {
                ok[k] = ok[i] = 1;
                st.pop();
            }
        } else {
            st.push(i);
        }
    }
    
    for (int i = 0; i < s.size(); i++) {
        if (ok[i]) {
            cout << s[i];
        } else {
            if (s[i] == '(' || s[i] == ')')
                cout << "()";
            if (s[i] == '[' || s[i] == ']')
                cout << "[]";
        }
    }
    return 0;
}
```





# 算法竞赛通用数据结构详解：树状数组、线段树与主席树

本文档针对算法竞赛（ACM/OI）场景，剔除了基础二叉树部分，重点解析 **树状数组 (BIT)** 的核心原理，并提供 **线段树** 和 **主席树** 的高效率通用 C++ 模板。

------



## 1. 树状数组 (Binary Indexed Tree, BIT)

树状数组是竞赛中最常用的数据结构之一，因为它**代码极短**、**常数极小**（比线段树快），专门用于解决**单点修改 + 区间前缀和**的问题。

### 1.1 核心原理与 Lowbit 详解

#### 为什么要用 Lowbit？

树状数组利用数的**二进制特征**来定义“管辖范围”。
lowbit(x) 用于提取 x 在二进制表示下最低位的 1 及其后面的 0 构成的数值。



- **公式**：lowbit(x) = x & -x

  - *原理*：计算机中负数使用补码表示 −x 等于 ~x + 1。例如 

    $x = 6 (0110_2)   $，则  $-x = 1010_2$   ，相与得 0010_2 = 2$。

  

#### 操作逻辑

1. **单点修改 (Add)**：x += lowbit(x)

   - 当你修改位置 

     ```
             xxx
           
     ```

      时，需要更新所有“管辖”了 

     ```
             xxx
           
     ```

      的父节点。

     

   - 操作就是不断寻找祖先节点，直到下标超出 

     ```
             nnn
           
     ```

     。

     

2. **区间查询 (Query)**：x -= lowbit(x)

   - 当你查 

     ```
             sum(x)sum(x)sum(x)
           
     ```

      时，需要把若干个子区间拼起来。

     

   - 操作就是不断减去当前区间的长度，跳到前一个区间的末尾，直到 

     ```
             x=0x=0x=0
           
     ```

     。

     

### 树状数组

```c++
#include <iostream>
#include <vector>
using namespace std;

// === 全局数组定义，防止爆栈 ===
const int MAXN = 200005; 
int tree[MAXN]; 
int n; // 数组实际大小

// === 核心宏定义 ===
#define lowbit(x) ((x) & -(x))

/**
 * 单点修改: a[x] += k
 * 复杂度: O(log n)
 * 注意: 树状数组下标必须从 1 开始
 */
void add(int x, int k) {
    for (; x <= n; x += lowbit(x)) {
        tree[x] += k;
    }
}

/**
 * 查询前缀和: sum = a[1] + ... + a[x]
 * 复杂度: O(log n)
 */
int query(int x) {
    int sum = 0;
    for (; x > 0; x -= lowbit(x)) {
        sum += tree[x];
    }
    return sum;
}

/**
 * 查询区间和: a[l] + ... + a[r]
 */
int query_range(int l, int r) {
    return query(r) - query(l - 1);
}

// === 使用示例 ===
int main_bit() {
    n = 5;
    // 模拟建立数组 [1, 2, 3, 4, 5]
    for(int i=1; i<=n; i++) add(i, i);
    
    // 查询区间 [2, 4] -> 2+3+4 = 9
    cout << query_range(2, 4) << endl; 
    
    // 修改: 第3个数 +10 -> [1, 2, 13, 4, 5]
    add(3, 10);
    
    // 再次查询 -> 2+13+4 = 19
    cout << query_range(2, 4) << endl;
    return 0;
}
```

------



## 2. 线段树 (Segment Tree)



```c++
#include <iostream>
using namespace std;
using ll = long long;
#define endl '\'

const int MAXN = 100005;
ll arr[MAXN];      // 原数组
ll tree[MAXN * 4]; // 线段树数组
ll tag[MAXN * 4];  // 懒标记

// 向上更新 (Push Up): 用子节点算父节点
void push_up(ll p) {
    tree[p] = tree[p << 1] + tree[p << 1 | 1];
}

// 向下下放 (Push Down): 核心逻辑
// p: 当前节点, len: 当前节点管辖的区间长度
void push_down(ll p, ll len) {
    if (tag[p]) {
        // 1. 传给左子 (p<<1) 和 右子 (p<<1|1)
        tag[p << 1] += tag[p];
        tag[p << 1 | 1] += tag[p];
        
        // 2. 更新子节点的值 (增量 = tag * 区间长度)
        tree[p << 1] += tag[p] * (len - len / 2);
        tree[p << 1 | 1] += tag[p] * (len / 2);
        
        // 3. 清除当前标记
        tag[p] = 0;
    }
}

// 建树
void build(ll p, ll l, ll r) {
    tag[p] = 0;
    if (l == r) {
        tree[p] = arr[l];
        return;
    }
    ll mid = (l + r) >> 1;
    build(p << 1, l, mid);
    build(p << 1 | 1, mid + 1, r);
    push_up(p);
}

// 区间修改: [ql, qr] 范围全加 k
void update(ll p, ll l, ll r, ll ql, ll qr, ll k) {
    // 1. 完全覆盖，直接更新并打标，不递归
    if (ql <= l && r <= qr) {
        tree[p] += k * (r - l + 1);
        tag[p] += k;
        return;
    }
    
    // 2. 未完全覆盖，先下放标记，再递归
    push_down(p, r - l + 1);
    ll mid = (l + r) >> 1;
    if (ql <= mid) update(p << 1, l, mid, ql, qr, k);
    if (qr > mid) update(p << 1 | 1, mid + 1, r, ql, qr, k);
    push_up(p);
}

// 区间查询: 求 [ql, qr] 的和
ll query(ll p, ll l, ll r, ll ql, ll qr) {
    if (ql <= l && r <= qr) return tree[p];
    
    push_down(p, r - l + 1); // 查之前也要下放
    ll mid = (l + r) >> 1;
    ll res = 0;
    if (ql <= mid) res += query(p << 1, l, mid, ql, qr);
    if (qr > mid) res += query(p << 1 | 1, mid + 1, r, ql, qr);
    return res;
}
```

------



## 3. 主席树 (可持久化线段树)



主要用于**查询历史版本**和**静态区间第 K 小**问题。
它利用了“单点修改只改变 $logn$个节点”的特性，每次修改时新建一条链，其余部分与上一个版本共用节点。



```c++
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

const int MAXN = 200005;

struct Node {
    int l, r; // 左右子节点的索引
    int cnt;  // 值域区间内数字的个数
} tr[MAXN * 25]; // 动态开点，空间约为 N * logN

int root[MAXN]; // 存储每个版本的根节点
int idx = 0;    // 节点分配器
vector<int> v;  // 离散化用的数组

// 离散化: 获取数值 x 对应的排名
int get_id(int x) {
    return lower_bound(v.begin(), v.end(), x) - v.begin() + 1;
}

// 插入操作: 生成新版本
// prev: 上个版本的节点, l/r: 值域范围, val: 插入值的排名
int insert(int prev, int l, int r, int val) {
    int p = ++idx;    // 新建节点
    tr[p] = tr[prev]; // 先复制旧版本信息
    tr[p].cnt++;      // 当前节点覆盖数+1
    
    if (l == r) return p;
    
    int mid = (l + r) >> 1;
    if (val <= mid) 
        tr[p].l = insert(tr[prev].l, l, mid, val); // 递归左边，右边复用
    else 
        tr[p].r = insert(tr[prev].r, mid + 1, r, val); // 递归右边
        
    return p;
}

// 查询操作: 找区间 [L, R] 第 k 小
// p: root[L-1], q: root[R] (利用前缀和思想作差)
int query(int q, int p, int l, int r, int k) {
    if (l == r) return l;
    
    // 计算左子树数字增量
    int cnt_left = tr[tr[q].l].cnt - tr[tr[p].l].cnt;
    
    int mid = (l + r) >> 1;
    if (k <= cnt_left)
        return query(tr[q].l, tr[p].l, l, mid, k);
    else
        return query(tr[q].r, tr[p].r, mid + 1, r, k - cnt_left);
}

// === 使用示例 ===
int main_chairman() {
    // 原始数据: 5, 2, 6, 3, 1
    vector<int> a = {5, 2, 6, 3, 1};
    int n = a.size();
    
    // 1. 离散化
    v = a;
    sort(v.begin(), v.end());
    v.erase(unique(v.begin(), v.end()), v.end());
    int max_sz = v.size();

    // 2. 构建版本 (root[i] 代表前 i 个数构成的树)
    for (int i = 0; i < n; i++) {
        root[i + 1] = insert(root[i], 1, max_sz, get_id(a[i]));
    }

    // 3. 查询: 原数组下标 1-based 区间 [2, 4] ({2, 6, 3}) 的第 2 小
    int L = 2, R = 4, k = 2;
    int val_idx = query(root[R], root[L - 1], 1, max_sz, k);
    
    // 输出应为 3 (2, 3, 6 中 3 是第2小)
    cout << "Result: " << v[val_idx - 1] << endl; 
    return 0;
}
```



##字典树(也称为 **前缀树**)

**利用字符串的公共前缀来减少查询时间，最大限度地减少无谓的字符串比较。**

#### 特性：

1. **根节点**不包含字符，除根节点外每一个节点都只包含一个字符。
2. 从根节点到某一节点，路径上经过的字符连接起来，为该节点对应的字符串。
3. **公共前缀共享节点**：比如 "abc" 和 "abd"，它们的 "ab" 部分在树上是共用节点的

```c++
const int N = 100010; // 根据题目总字符长度设定
int trie[N][26];       // 假设只存小写字母，每个节点最多26个子节点
int cnt[N];           // 计数器
int idx;              // 节点分配器，从0或1开始

// 插入字符串
void insert(string s) {
    int p = 0; // p代表当前所在的节点编号，0是根节点
    for (int i = 0; i < s.size(); i++) {
        int u = s[i] - 'a'; // 将字符转化为 0-25 的数字
        if (!trie[p][u]) trie[p][u] = ++idx; // 如果没有路，这就新建一条路
        p = trie[p][u]; // 走到下一个节点
        cnt[p]++;      // 【重点】这里记录有多少个单词经过了这个节点
    }
}

// 查询前缀出现次数（例如查 "ca" 是多少个单词的前缀）
int query(string s) {
    int p = 0;
    for (int i = 0; i < s.size(); i++) {
        int u = s[i] - 'a';
        if (!trie[p][u]) return 0; // 路断了，说明不存在这个前缀
        p = trie[p][u];
    }
    return cnt[p]; // 返回经过这个节点的数量
}
```

